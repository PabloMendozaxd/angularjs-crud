'use strict';

angular.
  module('angularjsCrud').
  component('userForm', {
    templateUrl: 'user-form/user-form.template.html',
    controller: function UserFormController($http) {
      var self = this;
      
      // $http.get('users/users.json').then(function(response) {
      // });
    }
  });
